// duoduo reconstruction — subsystem: 10-runtime-host
// symbol: resolveRuntimePaths  (minified: M9e, daemon.pretty.js:55501)
// NOTE: readable extract from daemon.recon.js; references other top-level
// symbols. The runnable artifact is recon/daemon.recon.js (provably equivalent).

function resolveRuntimePaths(e = {}, t = process.env) {
    let n = e.homeDir ?? t.ALADUO_HOME ?? D9e.homedir(),
        r = e.runtimeDir ?? t.ALADUO_RUNTIME_DIR ?? mt.join(n, ".aladuo"),
        i = e.kernelDir ?? t.ALADUO_KERNEL_DIR ?? mt.join(n, "aladuo"),
        s = e.workDir ?? t.ALADUO_WORK_DIR ?? process.cwd(),
        o = e.bootstrapDir ?? t.ALADUO_BOOTSTRAP_DIR ?? mt.join(L9e, "bootstrap"),
        a = mt.join(i, "config"),
        u = mt.join(r, "var"),
        c = mt.join(r, "run"),
        l = mt.join(u, "events"),
        d = mt.join(l, "index"),
        p = mt.join(u, "registry"),
        f = mt.join(u, "outbox"),
        m = mt.join(u, "generated"),
        h = mt.join(u, "sessions"),
        _ = mt.join(u, "jobs"),
        b = mt.join(u, "channels"),
        v = mt.join(u, "ingress"),
        w = mt.join(u, "telemetry"),
        g = mt.join(u, "usage"),
        x = mt.join(p, "sessions"),
        k = mt.join(i, "memory"),
        E = mt.join(k, "entities"),
        R = mt.join(k, "topics"),
        $ = mt.join(k, "fragments"),
        I = mt.join(k, "state"),
        P = mt.join(k, "CLAUDE.md"),
        C = mt.join(I, "meta-memory-state.json"),
        L = mt.join(u, "cadence"),
        G = mt.join(L, "inbox"),
        K = mt.join(L, "queue.md"),
        Q = mt.join(i, "subconscious"),
        W = mt.join(u, "subconscious"),
        ae = mt.join(Q, "playlist.md"),
        Oe = mt.join(u, "meta"),
        X = mt.join(Oe, "partitions"),
        Ue = mt.join(c, "locks"),
        Nt = mt.join(c, "queue_offsets"),
        Se = mt.join(c, "spine.sock"),
        st = mt.join(c, "daemon.stdout.log"),
        ze = mt.join(c, "daemon.stderr.log"),
        A = mt.join(o, "meta-prompt.md"),
        z = mt.join(n, ".claude", "CLAUDE.md");
    return {
        homeDir: n,
        runtimeDir: r,
        kernelDir: i,
        workDir: s,
        bootstrapDir: o,
        channelConfigDir: a,
        varDir: u,
        runDir: c,
        eventsDir: l,
        eventsIndexDir: d,
        registryDir: p,
        outboxDir: f,
        generatedDir: m,
        sessionsDir: h,
        jobsDir: _,
        channelsDir: b,
        varIngressDir: v,
        telemetryDir: w,
        usageDir: g,
        registrySessionsDir: x,
        memoryDir: k,
        memoryEntitiesDir: E,
        memoryTopicsDir: R,
        memoryFragmentsDir: $,
        memoryStateDir: I,
        memoryBroadcastPath: P,
        memoryMetaStatePath: C,
        cadenceDir: L,
        cadenceInboxDir: G,
        cadenceQueuePath: K,
        subconsciousDir: Q,
        subconsciousVarDir: W,
        subconsciousPlaylistPath: ae,
        partitionStateDir: X,
        runLocksDir: Ue,
        runQueueOffsetsDir: Nt,
        spineSocketPath: Se,
        daemonStdoutLogPath: st,
        daemonStderrLogPath: ze,
        metaPromptSrcPath: A,
        metaPromptDstPath: z
    }
}
