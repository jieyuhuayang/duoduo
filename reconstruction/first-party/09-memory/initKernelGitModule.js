// duoduo reconstruction — subsystem: 09-memory
// symbol: initKernelGitModule  (minified: qwe, daemon.pretty.js:69023)
// name: INFERRED — hand-derived from the body, not upstream's name (maps/inferred_daemon.json)
// NOTE: readable extract from daemon.recon.js; references other top-level
// symbols. The runnable artifact is recon/daemon.recon.js (provably equivalent).

var jwe, Lwe, rdt, initKernelGitModule = O(() => {
    "use strict";
    dt();
    jwe = ndt(edt), Lwe = `# Runtime state (not part of cognitive evolution)
memory/state/
memory/fragments/
memory/effectiveness/
subconscious/inbox/
.claude/
*.tmp
CLAUDE.local.md
# Model-profile config can embed credential-shaped values; keep it out of history
config/runtime.md
`, rdt = Lwe.split(`
`).map(e => e.trim()).filter(e => e.length > 0 && !e.startsWith("#"))
});
